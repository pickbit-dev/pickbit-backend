spring:
  config:
    activate:
      on-profile: develop

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Rlawhdgns4!
    url: jdbc:mysql://localhost:3306/pickbit_auth_server?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&allowPublicKeyRetrieval=true

  data:
    redis:
      host: localhost
      port: 6379

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:google-client-secret}
            redirect-uri: "${OAUTH_REDIRECT_BASE_URL:http://192.168.20.70:8080}/api/auth/oauth2/code/{registrationId}"
            scope:
              - email
              - profile
          kakao:
            provider: kakao
            client-id: ${KAKAO_CLIENT_ID:24d64f15c3bbf660398dc6631186c76a}
            client-secret: ${KAKAO_CLIENT_SECRET:p7rc26sMqjLF1c7Sp96nohjui5TO31LJ}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "${OAUTH_REDIRECT_BASE_URL:http://192.168.20.70:8080}/api/auth/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
            client-name: Kakao
          naver:
            provider: naver
            client-id: ${NAVER_CLIENT_ID:HxeWlZ4OLcjpNJ38mPHA}
            client-secret: ${NAVER_CLIENT_SECRET:CuHRoEAgH1}
            authorization-grant-type: authorization_code
            redirect-uri: "${OAUTH_REDIRECT_BASE_URL:http://192.168.20.70:8080}/api/auth/oauth2/code/{registrationId}"
            scope:
              - email
              - name
            client-name: Naver
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  cloud:
    consul:
      enabled: true
      host: localhost
      port: 8500

  jpa:
    defer-datasource-initialization: false
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        use_sql_comments: false
        default_batch_fetch_size: 100
    database: mysql
    hibernate:
      ddl-auto: create

server:
  port: 8081

openapi:
  docs-export:
    enabled: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: false
