# Pickbit Backend Architecture

## 개요

`pickbit-backend`는 여러 도메인 서비스를 하나의 저장소에서 함께 관리하는 모노레포(monorepo) 기반 백엔드 프로젝트다. 프로젝트는 Gradle 멀티모듈 구조를 사용하며, 공통 모듈과 서비스 모듈을 명확하게 분리하는 방향으로 설계했다.

현재 저장소에는 다음과 같은 모듈이 포함되어 있다.

- `library`
- `user-service`
- `product-service`
- `auction-service`
- `payment-service`
- `notification-service`
- `file-service`
- `gateway-service`

각 서비스는 독립적으로 실행 가능한 Spring Boot 애플리케이션이며, `library`는 여러 서비스에서 공유하는 공통 코드 전용 모듈이다.

## 배경

프로젝트 초기 단계에서 가장 먼저 고민한 부분은 서비스 경계를 어떻게 나눌지와, 이를 어떤 저장소 구조로 관리할지였다. 사용자, 상품, 경매, 결제, 알림처럼 도메인이 분리되는 특성을 고려하면 장기적으로는 서비스별 책임을 나누는 편이 유지보수와 확장성 측면에서 유리하다고 판단했다.

다만 초기에 각 서비스를 완전히 별도 저장소로 분리하면 다음과 같은 비용이 빠르게 증가할 수 있다.

- 저장소 수 증가로 인한 관리 비용 상승
- 공통 코드 배포 및 버전 정합성 관리 부담
- 서비스 간 인터페이스 변경 시 동시 반영 어려움
- 초기 개발 속도 저하

그래서 현재 단계에서는 서비스 경계는 분리하되, 저장소는 하나로 유지하는 모노레포 방식을 선택했다. 즉, 아키텍처 관점에서는 도메인별 서비스 분리를 지향하면서도, 개발 운영 관점에서는 하나의 저장소에서 통합 관리하는 전략이다.

## 왜 모노레포를 선택했는가

현재 구조에서 모노레포를 선택한 이유는 다음과 같다.

### 1. 공통 코드 재사용이 쉽다

`library` 모듈을 통해 공통 DTO, 예외, 유틸리티, 향후 공통 응답 포맷이나 인증 관련 코드를 한 곳에서 관리할 수 있다. 별도 저장소를 두고 공통 라이브러리를 배포하는 방식보다 초기 생산성이 높고 구조가 단순하다.

### 2. 서비스 간 변경 사항을 함께 관리할 수 있다

멀티모듈 구조에서는 공통 모듈 변경과 서비스 모듈 변경을 같은 커밋 흐름 안에서 반영할 수 있다. 예를 들어 공통 DTO가 바뀌면 이를 사용하는 여러 서비스 코드를 동시에 수정하고 함께 검증하기 쉽다.

### 3. 초기 개발 속도가 빠르다

서비스가 여러 개로 나뉘어 있어도 저장소가 하나이기 때문에 프로젝트 부트스트래핑, 공통 설정 정리, 빌드 구성, CI 기초 작업을 한 번에 맞추기 좋다. 초기 단계에서 구조를 빠르게 안정화하는 데 유리하다.

### 4. 일관된 규칙을 적용하기 쉽다

버전 규칙, 패키지 규칙, 설정 파일 규칙, 프로파일 전략 등을 전체 모듈에 동일한 방식으로 적용할 수 있다. 이는 팀 단위 협업 시 진입 장벽을 낮추는 데 도움이 된다.

## 전체 구조

프로젝트는 루트 Gradle 프로젝트 아래에 공통 모듈과 서비스 모듈을 배치하는 전형적인 멀티모듈 구조를 따른다.

```text
pickbit-backend/
|- build.gradle
|- settings.gradle
|- library/
|- gateway-service/
|- user-service/
|- product-service/
|- auction-service/
|- file-service/
|- payment-service/
`- notification-service/
```

각 모듈의 역할은 다음과 같다.

- `library`
  - 공통 DTO, 예외, 유틸리티 등 공유 코드 관리
  - 실행 애플리케이션이 아닌 공용 라이브러리 모듈
- `gateway-service`
  - 외부 요청의 단일 진입점
  - 인증 토큰 검증, 인증 헤더 전파, Consul 기반 라우팅, Redis 기반 rate limit 처리
- `user-service`
  - 사용자 관련 도메인 처리
- `product-service`
  - 상품 관련 도메인 처리
- `auction-service`
  - 경매 관련 도메인 처리
- `file-service`
  - 상품 이미지 등 파일 업로드 처리
  - Object Storage 연동 담당
- `payment-service`
  - 결제 관련 도메인 처리
- `notification-service`
  - 알림 관련 도메인 처리

## 개발 인프라 구성

로컬 개발 환경은 `docker-compose.dev.yml`로 공통 인프라를 띄운다.

- MySQL: `localhost:13306`
  - 각 도메인 서비스의 데이터베이스 저장소
  - Debezium outbox CDC를 위해 binlog 설정 활성화
- Consul: `localhost:18500`
  - 서비스 디스커버리
  - Gateway의 동적 라우팅 메타데이터 제공
- Redis: `localhost:16379`
  - `auth-service`의 refresh token, OAuth 임시 code 저장
  - `gateway-service`의 Redis rate limiter 저장소
  - `auction-service`의 Redisson 입찰 락, ShedLock, 경매 상세 캐시, WebSocket Pub/Sub 브리지
- Kafka: `localhost:19092`
  - 서비스 간 이벤트 전달
- Kafka Connect: `localhost:18088`
  - Debezium outbox connector 실행
- Kafka UI: `localhost:19090`
  - Kafka topic 및 connector 상태 확인

Redis는 컨테이너 내부 기본 포트 `6379`를 사용하지만, 로컬 호스트에서는 기존 Redis와 충돌하지 않도록 `16379`로 노출한다. 따라서 `develop` 프로파일의 Redis 설정은 `localhost:16379`를 기준으로 맞춘다.

## 빌드 및 의존성 관리 전략

루트 `build.gradle`에서는 모든 모듈에 공통으로 필요한 최소 설정만 관리하고 있다.

- 공통 `group`: `com.pickbit`
- Java Toolchain 공통 적용
- Maven Central 저장소 사용
- 테스트 관련 의존성 공통 적용

반면 서비스 모듈과 공통 모듈의 역할은 의존성 수준에서도 분리했다.

- `library`
  - `java-library` 기반 공통 모듈
  - Spring Boot 실행 모듈이 아님
- 서비스 모듈
  - Spring Boot 플러그인 적용 대상
  - `library`를 의존하여 공통 코드 재사용

이 구조를 정리하는 과정에서 중요한 기준은 `library`를 가능한 한 가볍고 독립적인 공통 모듈로 유지하는 것이었다. 그래서 `spring-boot-starter-web` 같은 실행 서비스용 의존성은 서비스 모듈에만 적용하고, `library`에는 넣지 않는 방식으로 정리했다.

예를 들어 `product-service`는 서비스 특성상 영속성 계층이 필요하므로 다음과 같은 의존성을 개별 선언한다.

- `org.springframework.boot:spring-boot-starter-data-jpa`
- `com.mysql:mysql-connector-j`

즉, 공통 의존성은 루트에서 최소한으로 관리하고, 도메인 특화 의존성은 각 서비스 모듈에서 직접 선언하는 전략을 사용한다.

## 버전 관리 방식

버전은 루트 프로젝트에서 일괄 관리하지 않고 각 모듈의 `build.gradle`에서 개별 선언하도록 정리했다.

이 방식의 목적은 서비스별 릴리스 주기를 독립적으로 가져가기 위함이다. 모든 모듈이 항상 같은 버전으로 배포된다는 보장이 없기 때문에, 서비스별로 필요한 시점에 버전을 올릴 수 있도록 설계했다.

정리하면 다음과 같다.

- `group`은 루트에서 공통 관리
- `version`은 모듈별 개별 관리

이렇게 하면 특정 서비스만 변경되었을 때 해당 모듈의 버전만 조정하면 되고, 변경 범위가 명확해진다.

## 설정 파일 관리 전략

환경설정은 기존 `application.properties` 방식 대신 `application.yml` 기반으로 통일했다. YAML 형식을 선택한 이유는 계층 구조를 표현하기 쉽고, Spring 설정을 읽고 관리하기 편하기 때문이다.

현재는 각 서비스별로 다음 파일 구조를 기준으로 설정을 나누고 있다.

- `application.yml`
- `application-develop.yml`
- `application-deploy.yml`
- `application-test.yml`

역할은 다음과 같이 구분한다.

- `application.yml`
  - 서비스명, 공통 옵션 등 환경과 무관한 설정
- `application-develop.yml`
  - 로컬 개발 환경 설정
- `application-deploy.yml`
  - 배포 환경 설정
- `application-test.yml`
  - 테스트 환경 설정

이 구조를 통해 공통값과 환경별 값을 분리하고, 개발/테스트/운영 간 설정 충돌을 줄이도록 했다.

## 프로파일 전략

현재 운영하는 프로파일은 다음 세 가지다.

- `develop`
- `deploy`
- `test`

프로파일을 나눈 이유는 각 환경이 요구하는 설정이 명확히 다르기 때문이다. 예를 들어 개발 환경은 빠른 확인과 로컬 실행 편의성이 중요하고, 테스트 환경은 독립적인 검증이 중요하며, 배포 환경은 안정성과 보안이 중요하다.

따라서 같은 서비스라도 프로파일에 따라 포트, 데이터베이스 설정, 로깅 수준, JPA 옵션 등을 다르게 가져갈 수 있도록 준비했다.

## 시크릿 관리 방향

민감한 값은 일반 설정 파일과 분리해서 관리하는 방향으로 정리하고 있다. 특히 개발 환경에서는 `application-develop.yml`과 별도로 시크릿 파일을 두어 DB URL, 사용자 계정, 비밀번호 같은 정보를 따로 분리하는 방식을 고려하고 있다.

기본 원칙은 다음과 같다.

- Git에 포함되는 설정 파일에는 비민감 정보만 둔다
- 비밀번호, API Key, 토큰, 외부 연동 키는 별도 시크릿 파일 또는 환경변수로 분리한다
- 장기적으로는 운영 환경에서 환경변수 또는 Secret Manager 기반 관리로 확장 가능하도록 한다

즉, 설정 파일은 구조와 동작 정책을 설명하는 역할을 맡고, 실제 민감값은 외부 주입 또는 별도 관리 파일로 다루는 방향이다.

## 현재 아키텍처의 의도

현재 구조는 완전한 분산 환경을 당장 구축하기보다는, 도메인 경계를 미리 서비스 단위로 나누고 이를 점진적으로 발전시킬 수 있도록 설계한 형태다. 다시 말해, 지금은 모노레포 기반 멀티모듈 구조이지만 장기적으로는 필요에 따라 서비스별 독립 배포나 운영 전략으로 발전시킬 수 있는 기반을 먼저 다지는 단계다.

이 아키텍처가 지향하는 핵심은 다음과 같다.

- 도메인별 책임 분리
- 공통 코드의 일관된 재사용
- 초기 개발 생산성 확보
- 설정과 빌드 규칙의 표준화
- 향후 서비스 독립성 확보를 위한 기반 마련

## 정리

현재 `pickbit-backend`는 Gradle 멀티모듈 기반의 모노레포 백엔드 구조를 채택했다. 공통 모듈인 `library`와 독립 실행 서비스 모듈들을 분리해 두었고, 빌드 설정, 의존성, 버전, 프로파일, 설정 파일, 시크릿 관리 기준을 모듈 구조에 맞게 정리하고 있다.

즉, 지금 단계의 목표는 다음과 같다.

- 도메인별 서비스를 물리적으로 분리된 모듈로 구성한다
- 저장소는 하나로 유지해 초기 복잡도를 낮춘다
- 공통 코드는 `library`로 모은다
- 서비스별 설정과 의존성은 각 모듈에서 관리한다
- 환경별 설정과 민감값 관리를 분리한다

결과적으로 이 구조는 초기 개발 속도와 공통 관리 효율을 확보하면서도, 이후 서비스가 성장했을 때 더 독립적인 운영 구조로 확장할 수 있도록 설계된 백엔드 아키텍처라고 볼 수 있다.
